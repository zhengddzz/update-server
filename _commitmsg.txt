app.html 下载卡片改为密集列表，提高信息密度

- "当前版本下载" 改为 Fraunces 大标题（醒目），副标识显示 Current Release · vX.Y.Z
- 每个安装包从独立卡片改为单行：format | version | size | filename | [下载按钮] | SHA（截断）
- URL 隐藏在下载按钮内（不直接展示完整链接，避免视觉噪音）
- SHA-256 默认显示前 12 位 + …，点击复制完整值，带"已复制 ✓"反馈
- 平台分组标题加圆点装饰 + 下划线分隔
- 移动端响应式优化：dl-row 自动换行
- "版本历史" 同步改为大标题 + Changelog 副标识
